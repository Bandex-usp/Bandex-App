<resources xmlns:android="http://schemas.android.com/apk/res/android">

<!--
    <style name="AppTheme" parent="@android:style/Theme.Holo.Light">
        <item name="actionMenuTextColor">@color/black</item>
        <item name="android:actionBarItemBackground">@drawable/actionbar_background</item>
    </style>
-->
    <style name="AppTheme"
        parent="@style/Theme.AppCompat.Light.DarkActionBar">
        <item name="android:actionBarStyle">@style/MyActionBar</item>
        <item name="actionMenuTextColor">@color/black</item>
        <!-- Support library compatibility -->
        <item name="actionBarStyle">@style/MyActionBar</item>
    </style>

    <!-- ActionBar styles -->
    <style name="MyActionBar"
        parent="@style/Theme.AppCompat.Light.DarkActionBar">
        <item name="logo">@drawable/abc</item>
        <item name="displayOptions">useLogo|showTitle</item>
        <item name="android:background">@drawable/actionbar_background</item>

        <!-- Support library compatibility -->
        <item name="background">@drawable/actionbar_background</item>
    </style>


        <style name="Widget.ActionBar" parent="@style/Theme.AppCompat.Light.DarkActionBar">
            <!-- ... -->
        </style>

        <style name="Widget.ActionBar.Logo">
            <item name="android:logo">@drawable/usp_logo</item>
            <item name="android:displayOptions">useLogo|showHome</item>
        </style>
</resources>
